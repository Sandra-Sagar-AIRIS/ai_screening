# Local dev: same-origin path (Next.js rewrites /api/v1 → FastAPI). Production: set full API URL.
NEXT_PUBLIC_API_BASE_URL=/api/v1
API_PROXY_TARGET=http://127.0.0.1:8000
NEXT_PUBLIC_USE_CANDIDATE_MANAGEMENT=false
